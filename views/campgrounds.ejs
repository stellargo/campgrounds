<% include partials/header %>
<% campgrounds.forEach(function(campground){ %>
	<h1> <%= campground.name %> </h1>	
	<img src= <%=campground.image %> width=200px>
<% }); %>
<% include partials/footer %>